# مواصفات PWA العامة الثابتة

## الغرض والنطاق

هذه النسخة هي بناء React ثابت لواجهة اللعبة الحديثة، منشور من فرع `gh-pages` في المستودع العام. لا يوجد فيها خادم أو قاعدة بيانات أو حسابات، وتظل بيانات الجولة محفوظة في `localStorage` الخاص بالمتصفح فقط.

| المجال | الحالة في النسخة العامة | ملاحظات الأمان والخصوصية |
|---|---|---|
| لعبة البطاقات والكتالوج المعتمد | متاح محلياً | يستخدم محتوى اللعبة المضمن في الحزمة؛ لا يحمل إضافات مجتمع أو بيانات خاصة. |
| بدء الجلسة وحفظها واستئنافها | متاح محلياً | الأسماء وحالة الجولة لا تغادر الجهاز؛ تستأنف الجولة النشطة تلقائياً بعد إعادة التحميل، ويمكن بدء الجولة من دون أسماء باستخدام «اللاعب الأول» و«اللاعب الثاني» تلقائياً. |
| زر «نصيحة» | متاح | يظهر في الشريط العلوي ويعرض نصيحة واحدة عشوائية عند الطلب؛ لا توجد قائمة أو سجل نصائح ظاهر. |
| صفحة التوثيق | متاحة للقراءة | تعرض سجل المراجع والحوكمة العام المضمن في البناء. |
| التثبيت والعمل دون اتصال | متاح بعد أول زيارة | manifest، أيقونات 192/512، وعامل خدمة محدود النطاق `/hadith-alqulub/`. |
| الحسابات وتسجيل الدخول | غير متاح | لا توجد OAuth أو جلسات أو أزرار حساب في الحزمة. |
| اقتراحات المستخدمين والمراجعة والنشر | غير متاح | لا تكتب الاقتراحات أو قرارات الإدارة في `localStorage` بديلاً عن الخادم. |
| الإدارة والأرشفة وقاعدة البيانات | غير متاح | تظل حصراً في المنصة الخاصة المتصلة. |

## التخزين المؤقت والتحديث

يستخدم عامل الخدمة ذاكرة باسم `hadith-alqulub-modern-v4`. يخزن فقط غلاف التطبيق وملفات JavaScript/CSS والأيقونات العامة ضمن نطاق GitHub Pages. لا يخزن استجابات مصادقة أو بيانات حسابات أو طلبات تعديل، لأن هذه النسخة لا ترسل تلك الطلبات أصلاً.

يبقى عامل الخدمة الجديد في حالة انتظار عند توفر إصدار أحدث حتى يختار المستخدم تطبيق التحديث من الواجهة؛ لا يفرض إعادة تحميل مفاجئة أثناء الجلسة.

## التحقق المطلوب بعد كل إصدار

بعد الزيارة الأولى عبر الإنترنت، افحص بدء جلسة باسمين، فتح بطاقة، استخدام زر النصيحة، وإعادة التحميل مع قطع الاتصال. اختبر كذلك التثبيت اليدوي على هاتف حقيقي، لأن توافر زر التثبيت يختلف بين المتصفحات وأنظمة التشغيل.
