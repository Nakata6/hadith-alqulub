# برومبت استعادة الميزات الخادمية مستقبلاً

> أريد تطوير **المنصة الكاملة الخاصة** في مستودع `Nakata6/hadith-alqulub-platform`، وليس تحويل فرع GitHub Pages العام إلى خادم. حافظ على `Nakata6/hadith-alqulub` كتطبيق PWA ثابت للأوفلاين من دون حسابات أو API.

أضف/عدّل ميزة خادمية فقط في المنصة الخاصة وفق الشروط التالية:

1. استخدم خادم Express وtRPC وقاعدة البيانات والمصادقة الموجودة في المنصة الخاصة. لا تضع مفاتيح أو OAuth أو بيانات حسابات أو قرارات مراجعة في فرع `gh-pages` أو `localStorage`.
2. أبقِ صلاحيات المستخدم والمالك مفروضة على الخادم؛ لا تعتمد على إخفاء أزرار الواجهة للحماية.
3. عند إضافة اقتراحات أو مراجعة أو أرشفة أو نشر، عرّف الجداول والإجراءات والتحقق من الإدخال والاختبارات أولاً، ثم اربط واجهة React بها.
4. لا تُدرج نصوصاً دينية جديدة أو تغيّر درجاتها أو مصادرها من دون المرور بملفات الحوكمة وسجل الشفافية المعتمدين. لا تقتطع المتن، ولا تنسب درجة غير منشورة.
5. أبقِ النسخة العامة متزامنة فقط عبر `pnpm build:static-pwa` من مصدر الواجهة الحديث، ثم انشر **ملفات `dist/static-pwa` فقط** إلى فرع `gh-pages`. لا تنسخ ملفات الخادم أو `.env` أو قاعدة البيانات أو مخرجات المنصة الكاملة إلى المستودع العام.
6. قبل أي نشر، شغّل اختبارات الوحدة وفحص TypeScript والبناء، ثم اختبر تدفق المستخدم والصلاحيات يدوياً. حافظ على عامل الخدمة محدود النطاق وعلى سياسة عدم تخزين البيانات الخاصة.
